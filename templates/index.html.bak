<!DOCTYPE html>
<html>
  <head>
    <title>New Event Detection - Twitter</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="//netdna.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet" media="screen">
    <style>
      .container {
        max-width: 1000px;
      }
    </style>
  </head>
{% extends "layout.html" %}
{% block body %}  
  <body width="100%">
    <div width="100%" class="container">
      <h1>New Event Detection - Twitter</h1>
      <form role="form" method='POST' action='/'>
        <div width="100%" class="form-group">
          <table width="400px">
	      		{% for section in params %}
							    <tr width="100%">
							        <td colspan="3" class="divider"><div style="background-color: lightblue;" >{{ params[section].title }}</div></td>
							    </tr>
							{% set temp = params[section] %}    
		      		{% for param in temp %}
		      			{% if (param != "title") %}
				      		<tr width="100%">
					          <td width="40%">{{temp[param].label}}:</td>
					          <td width="10%">&nbsp;</td>
					          <td width="50%"><input width="100%" type="text" name="{{param}}" class="form-control" id="{{param}}-box" placeholder="" style="max-width: 100px;" autofocus required value="{{temp[param].value}}"></td>
				          </tr>
								{% endif %}
		      		{% endfor %}
	      		

				    
				    {% endfor %}
      		</table>
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      <br>
      
      {% for tweet in tweets %}
        <h4 >{{ tweet._id }}</h4>
      {% endfor %}
      
      <br/>
      {% for error in errors %}
        <h4 style='font-color:red'>{{ error }}</h4>
      {% endfor %}

    </div>
    <script src="//code.jquery.com/jquery-2.2.1.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
  </body>
{% endblock %}  
</html>
